{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs only. All filesystem and Godot work goes through app commands (invoke), which need no plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
